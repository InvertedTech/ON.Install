name: StripePayments
services:
  - name: nodeclient
    dockerFile: ON.Authorization/StripePayments/Dockerfile
    dockerFileContext: ON.Authorization/StripePayments/
    bindings:
      - port: 3000
    env:
      - name: PORT
        value: 3000
