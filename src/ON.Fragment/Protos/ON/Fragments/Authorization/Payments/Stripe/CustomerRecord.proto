syntax = "proto3";

package ON.Fragments.Authorization.Payments.Stripe;

message CustomerRecord {
	bytes GUID = 1;
	string CustomerId = 2;
}

message CustomerList {
	repeated CustomerRecord Records = 1;
}